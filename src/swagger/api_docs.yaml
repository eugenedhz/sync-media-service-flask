################################################################################
#                              API Information                                 #
################################################################################
info:
  title: 'ilow Media Service API DOCS'
  description: |
      ## USAGE

      Just scroll down to the **interactive API requests**!
      
      -----------------------------------------------

      ## REQUEST HTTP METHODS 

      - To `create` something use ***POST*** method

      - To `get` something use ***GET*** method

      - To `update` something use ***PATCH*** method

      - To `delete` something use ***DELETE*** method

      -----------------------------------------------

      ## ***API ERRORS***

      📝 **Note:** Some errors may have human-readable `description` field.
      
      | Status Code | Status Description | Message |
      | --- | --- | --- |
      | ***Global errors*** |
      | 405 | METHOD NOT ALLOWED | ***METHOD_NOT_ALLOWED*** |
      | 404 | NOT FOUND | ***RESOURCE_NOT_FOUND*** |
      | 500 | INTERNAL SERVER ERROR | ***INTERNAL_SERVER_ERROR*** |
      | 403 | FORBIDDEN | ***ADMIN_RIGHTS_REQUIRED*** |
      | ***JSON validation errors*** |
      | 400 | BAD REQUEST | ***"field_name": "MISSING_FIELD"*** |
      | 400 | BAD REQUEST | ***"field_name": "UNKNOWN_FIELD"*** |
      | 400 | BAD REQUEST | ***"field_name": "INVALID_LENGTH"*** |
      | 400 | BAD REQUEST | ***"field_name": "INVALID_RANGE"*** |
      | 400 | BAD REQUEST | ***"field_name": "REGEXP_MISMATCH"*** |
      | 400 | BAD REQUEST | ***"field_name": "INVALID_TYPE"*** |
      | 400 | BAD REQUEST | ***"field_name": "NOT_NULLABLE"*** |
      | ***Formdata errors*** |
      | 400 | BAD REQUEST | ***FORMDATA_IS_EMPTY*** |
      | ***Query params errors*** |
      | 400 | BAD REQUEST | ***INVALID_FILTER_BY*** |
      | 400 | BAD REQUEST | ***INVALID_SELECT*** |
      | 400 | BAD REQUEST | ***INVALID_ID*** |
      | 400 | BAD REQUEST | ***NO_IDENTITY_PROVIDED*** |
      | ***File uploading errors*** |
      | 400 | BAD REQUEST | ***INVALID_JPG*** |
      | 500 | INTERNAL SERVER ERROR | ***CANNOT_SAVE_FILE_ON_SERVER*** |
      | ***Authenticaton API errors*** |
      | 401 | UNAUTHORIZED | ***NO_TOKEN_PROVIDED*** |
      | 401 | UNAUTHORIZED | ***NO_FRESH_TOKEN_PROVIDED*** |
      | 401 | UNAUTHORIZED | ***TOKEN_HAS_EXPIRED*** |
      | 422 | UNPROCESSABLE ENTITY | ***INVALID_TOKEN*** |
      | 409 | CONFLICT | ***USERNAME_ALREADY_EXISTS*** |
      | 409 | CONFLICT | ***EMAIL_ALREADY_EXISTS*** |
      | 409 | CONFLICT | ***USERNAME_NOT_FOUND*** |
      | 409 | CONFLICT | ***INCORRECT_PASSWORD*** |
      | 403 | FORBIDDEN | ***USER_IS_BANNED*** |
      | 404 | NOT FOUND | ***USER_NOT_FOUND*** |
      | ***User API errors*** |
      | 409 | CONFLICT | ***USERNAME_ALREADY_EXISTS*** |
      | 409 | CONFLICT | ***EMAIL_ALREADY_EXISTS*** |
      | 404 | NOT FOUND | ***USERS_NOT_FOUND*** |
      | 404 | NOT FOUND | ***USER_NOT_FOUND*** |
      | ***Media API errors*** |
      | 404 | NOT FOUND | ***MEDIA_NOT_FOUND*** |
      | 404 | NOT FOUND | ***MEDIAS_NOT_FOUND*** |

      -----------------------------------------------
  
  version: 'beta 0.0.1'

  contact:
    url: https://vk.com/akumii
    email: toropkibn@gmail.com

  license:
    name: Nginx 1.18.0
    url: https://nginx.org/LICENSE

externalDocs:
  description: ilow Gitlab!
  url: https://gitlab.eugenv.ru

tags:
  - name: User Authentication API
    description: Do everything with authentication :)
  - name: User API
    description: Do everything with users :)
  - name: Media API
    description: Do everything with media :)
  - name: Video API
    description: Do everything with videos :)
  - name: Rooms API
    description: Do everything with rooms :)

################################################################################
#                            Hosts, Servers, cookies auth                      #
################################################################################

servers: [
  {
    "url": "https://ilow-api.eugenv.ru/",
    "description": "Development server"
  },
  {
    "url": "http://127.0.0.1:8302/",
    "description": "Local server"
  },
  {
    "url": "No production server is present...",
  }
]

################################################################################
#                                    Paths                                     #
################################################################################

paths:
  /auth/signup:
    post:
      summary: Регистрация пользователя

      tags:
        - User Authentication API

      operationId: signup

      requestBody:
        required: true
        description: Обязательный JSON
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SipnupSchema'

      responses:
        200:
          description: Пользователь зарегистрироан, возвращается его сущность с access-token и refresh-token для cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /auth/login:
    post:
      summary: Логин пользователя

      tags:
        - User Authentication API

      operationId: login

      requestBody:
        required: true
        description: Обязательный JSON
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginSchema'

      responses:
        200:
          description: Пользователь залогинился, возвращается его сущность с access-token и refresh-token для cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /auth/logout:
    post:
      security:
        - cookieAuth: []

      operationId: logout

      summary: Выход из приложения, сброс токенов из cookies.

      tags:
        - User Authentication API

      responses:
        200:
          description: Выводит {logout = true}, сбрасывает токены из cookies

  /auth/refresh:
    post:
      security:
        - cookieAuth: []

      operationId: refresh

      summary: Запросить новый токен, когда истечёт срок старого

      description: |
        Когда `access-token` для авторизации истекает, используется `refresh-token` для его обновления.
        Если `refresh-token` истекает: необходимо заново залогиниться.

      tags:
        - User Authentication API

      responses:
        200:
          description: Выводит {refresh = true}, ставит новый токен в cookies

  /user/all:
    get:

      operationId: get_users

      summary: Получить пользователей

      description: Если пользователи не найдены, возвращает пустой список.

      tags:
        - User API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля пользователя, например `select=id,username`
        type: string
      - in: query
        name: filter_by
        required: false
        description: |
          Отфильтровать пользователей по значениям полей, например `filter_by=displayName=Sasha,isBanned=false,avatar=null`.
        type: string

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /user:
    patch:
      security:
        - cookieAuth: []

      operationId: update_user

      summary: Пользователь обновляет данные аккаунта.

      tags:
        - User API

      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserUpdateSchema'

      responses:
        200:
          description: Пользователь обновляется, возвращается его сущность.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

    get:
      operationId: get_user

      summary: Получить пользователя

      tags:
        - User API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля пользователя, например `select=id,username`
        type: string
      - in: query
        name: id
        required: false
        description: Получить пользователя по id, принимается только id=число, иначе выведет ошибку.
        type: int
      - in: query
        name: username
        required: false
        description: Получить пользователя по username
        type: string

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_user

      summary: Удаление пользователя.

      description: Этим может воспользоваться и юзер без админ прав и админ, для удаления другого пользователя.

      tags:
        - User API

      parameters:
      - in: query
        name: id
        required: false
        description: Удалить пользователя по id, принимается только id=число, иначе выведет ошибку.
        type: int

      responses:
        200:
          description: Пользователь удаляется, возвращается его сущность.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /media:
    post:
      security:
        - cookieAuth: []

      summary: Создание медиа

      tags:
        - Media API

      operationId: create_media

      requestBody:
        required: true
        description: Обязательный JSON
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/MediaCreateSchema'

      responses:
        200:
          description: Объект медиа создан, возвращается его сущность.
          content:
            multipart/form-data:
              schema:
                $ref: '#/components/schemas/MediaSchema'

    get:
      summary: Получение медиа

      tags:
        - Media API

      operationId: get_media

      parameters:
        - in: query
          name: select
          required: false
          description: |
            Получить только определённые поля медиа, например `select=id,name`
          type: string
        - in: query
          name: id
          required: false
          description: Получить медиа по id, принимается только id=число, иначе выведет ошибку.
          type: int

      responses:
        200:
          description: Возвращается сущность объекта медиа.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaSchema'

    patch:
      security:
        - cookieAuth: []

      operationId: update_media

      summary: Обновление медиа.

      tags:
        - Media API

      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/MediaUpdateSchema'

      parameters:
        - in: query
          name: id
          required: false
          description: Обновить media по id, принимается только id=число, иначе выведет ошибку.
          type: int

      responses:
        200:
          description: Объект медиа обновляется, возвращается его сущность.
          content:
            multipart/form-data:
              schema:
                $ref: '#/components/schemas/MediaSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_media

      summary: Удаление медиа.

      description: Этим может воспользоваться админ для удаления медиа

      tags:
        - Media API

      parameters:
      - in: query
        name: id
        required: false
        description: Удалить media по id, принимается только id=число, иначе выведет ошибку.
        type: int

      responses:
        200:
          description: Объект медиа удаляется, возвращается его сущность.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaSchema'

  /media/all:
    get:

      operationId: get_medias

      summary: Получить все медиа

      description: Если медиа не найдены, возвращает пустой список.

      tags:
        - Media API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля медиа, например `select=id,name`
        type: string
      - in: query
        name: filter_by
        required: false
        description: |
          Отфильтровать медиа по значениям полей, например `filter_by=name=Барби,description=Почему всё розовое`.
        type: string

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaSchema'

  /upload/session:
    get:
      operationId: get_session_uuid
      summary: Получить uuid загрузочной сессии
      description: получение uuid сессии, если сессия некоторое время не использовалась, она удаляется, берётся на каждую загрузку отдельно
      tags:
        - Video API

      responses:
          200:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SessionSchema'

    delete:
      operationId: abort_session
      summary: отменить загрузочную сессию по её uuid
      tags:
        - Video API

      parameters:
      - in: query
        name: session
        required: true
        description: |
          uuid сессии

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AbortSchema'

  /upload:
    post:
      operationId: load_chunk
      summary: загрузить чанк файла
      tags:
        - Video API

      requestBody:
        required: true
        description: Обязательный JSON
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChunkSchema'

      responses:
        200:
          description: возвращается индекс загруженного чанка
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChunkUploadedSchema'

        201:
          description: при загрузке последнего чанка, возвращается имя загруженного файла (статус код тоже 200, просто сваггер не дал ещё второй сделать)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileUploadedSchema'

  /upload/transcode_status:
    get:
      operationId: get_transcode_status
      summary: получить статусы обработки видео
      tags:
        - Video API

      parameters:
      - in: query
        name: session
        required: true
        description: |
          uuid загрузочной сессии

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusesSchema'


################################################################################
#                                    Schemas                                   #
################################################################################


components:
  schemas:
    SipnupSchema:
      type: object
      properties:
        username:
          type: string
          nullable: false
          maxLength: 30
          minLength: 5
          description: |
            Уникальный username пользователя, имеет `regexp = ^[a-zA-Z0-9._-]+$`
          required: true
          example: "nick2004"
        password:
          type: string
          nullable: false
          minLength: 8
          description: |
            имеет `regexp = ^[a-zA-Z0-9.@_-]+$`
          required: true
          example: 'somepassword'
        email:
          type: string
          nullable: false
          minLength: 1
          required: true
          example: 'example@example.org'
        displayName:
          type: string
          nullable: false
          maxLength: 30
          minLength: 1
          description: Необязательно уникальное имя пользователя, которое будет показываться другим
          required: true
          example: 'Lolkek'

    LoginSchema:
      type: object
      properties:
        username:
          type: string
          nullable: false
          maxLength: 30
          minLength: 5
          description: |
            Уникальный username пользователя, имеет `regexp = ^[a-zA-Z0-9._-]+$`
          required: true
          example: "nick2004"

        password:
          type: string
          nullable: false
          minLength: 8
          description: |
            имеет `regexp = ^[a-zA-Z0-9.@_-]+$`
          required: true
          example: 'somepassword'

    UserUpdateSchema:
      type: object
      properties:
        username:
          type: string
          nullable: false
          maxLength: 30
          minLength: 5
          description: |
            Уникальный username пользователя, имеет `regexp = ^[a-zA-Z0-9._-]+$`
          required: false
          example: "nick2004"
        displayName:
          type: string
          minLength: 1
          maxLength: 30
          required: false
          example: 'Lolkek'
        email:
          type: string
          required: false
          minLength: 1
          example: 'example@example.org'
        birthday:
          type: integer
          required: false
          min: 86400
          description: 'timestamp'
          example: 1710230677
        description:
          type: string
          required: false
          minLength: 1
          maxLength: 140
          example: 'Описание страничкии'
        avatar:
          required: false
          type: string
          format: binary
          description: только .jpg

    UserSchema:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        username:
          type: string
          nullable: false
          example: 'nick2004'
        displayName:
          type: string
          nullable: false
          example: 'Lolkek'
        email:
          type: string
          nullable: false
          example: 'example@example.org'
        isBanned:
          type: boolean
          nullable: false
          example: false
        registrationDate:
          type: integer
          nullable: false
          description: 'timestamp'
          example: 1710230677
        birthday:
          type: integer
          nullable: true
          description: 'timestamp'
          example: 1710230677
        description:
          type: string
          nullable: true
          example: 'Описание страничкии'
        avatar:
          type: string
          nullable: true
          example: '/static/images/avatar_id_1.png'

    MediaSchema:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        name:
          type: string
          nullable: false
          example: 'Titanic'
        description:
          type: string
          nullable: false
          example: 'Описание фильма'
        thumbnail:
          type: string
          nullable: false
          example: '/static/images/thumbnail_film.png'
        preview:
          type: string
          nullable: false
          example: '/static/images/preview_film.png'
        trailer:
          type: string
          nullable: true
          example: '/static/videos/abc1.mp4'

    MediaUpdateSchema:
      type: object
      properties:
        name:
          minLength: 1
          maxLength: 50
          type: string
          nullable: false
          example: 'Titanic'
          description: При пустом поле остаётся прежнее значение.
          required: false
        description:
          minLength: 1
          maxLength: 200
          type: string
          nullable: false
          example: 'Описание фильма'
          required: false
        thumbnail:
          required: false
          type: string
          format: binary
          description: только .jpg. При пустом поле остаётся прежнее значение.
        preview:
          required: false
          type: string
          format: binary
          description: только .jpg. При пустом поле остаётся прежнее значение.
        trailer:
          minLength: 3
          required: false
          type: string
          nullable: false
          example: 'abc1.mp4'
          description: |
            В поле вставляется имя файла с расширением. `regexp = ^[0-9a-z]+\.[a-z0-9]+$`

    MediaCreateSchema:
      type: object
      properties:
        name:
          minLength: 1
          maxLength: 50
          type: string
          nullable: false
          example: 'Titanic'
          required: true
        description:
          minLength: 1
          maxLength: 200
          type: string
          nullable: false
          example: 'Описание фильма'
          required: true
        thumbnail:
          nullable: false
          required: true
          type: string
          format: binary
          description: только .jpg
        preview:
          nullable: false
          required: true
          type: string
          format: binary
          description: только .jpg
        trailer:
          minLength: 3
          required: false
          type: string
          nullable: false
          example: 'abc1.mp4'
          description: |
            В поле вставляется имя файла с расширением. `regexp = ^[0-9a-z]+\.[a-z0-9]+$`

    ChunkSchema:
      type: object
      properties:
        session:
          type: string
          nullable: false
          required: true
          description: |
            Перед загрузкой необходимо получить uuid сессии `(/upload/session [GET])`
        totalFileSize:
          type: integer
          nullable: false
          required: true
          min: 1
        totalChunkCount:
          type: integer
          nullable: false
          required: true
          min: 1
        chunkIndex:
          type: integer
          nullable: false
          required: true
          min: 0
        chunkByteOffset:
          type: integer
          nullable: false
          required: true
          min: 0
        chunk:
          type: string
          format: binary
          required: true
          nullable: false
          description: |
            файл чанка, доступные форматы: `.mp4`, `.mov`, `.avi`, `.m4v`

    SessionSchema:
      type: object
      properties:
        session:
          type: string
          nullable: false
          example: jrj2rij2oriej23i324
          description: uuid созданной сессии

    ChunkUploadedSchema:
      type: object
      properties:
        chunkUploaded:
          type: integer
          nullable: false
          example: 0
          description: индекс загруженного чанка

    FileUploadedSchema:
      type: object
      properties:
        video:
          type: string
          nullable: false
          example: video.mp4
          description: возвращается имя загруженного файла после последнего чанка

    AbortSchema:
      type: object
      properties:
        aborted:
          type: string
          nullable: false
          example: jrj2rij2oriej23i324
          description: uuid отменённой сессии

    StatusesSchema:
      type: object
      description: |
        может также возвращать статус `expired` у некоторых качеств, если они были завершены определённое время назад и удалены из сессии
      properties:
        360p:
          type: string
          nullable: false
          example: success
          description: статус успешного завершения обработки качества
        480p:
          type: string
          nullable: false
          example: error
          description: статус завершения обработки качества с ошибкой
        720p:
          type: string
          nullable: false
          example: processing
          description: статус идущей обработки качества
        1080p:
          type: string
          nullable: false
          example: pending
          description: статус ожидания обработки качества
