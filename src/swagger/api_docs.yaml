################################################################################
#                              API Information                                 #
################################################################################
info:
  title: 'ilow Media Service API DOCS'
  description: |
      ## USAGE

      Just scroll down to the **interactive API requests**!
      
      -----------------------------------------------

      ## REQUEST HTTP METHODS 

      - To `create` something use ***POST*** method

      - To `get` something use ***GET*** method

      - To `update` something use ***PATCH*** method

      - To `delete` something use ***DELETE*** method

      -----------------------------------------------

      <details>
        <summary> ***API ERRORS*** </summary>

        📝 **Note:** Some errors may have human-readable `description` field.
        
        | Status Code | Status Description | Message |
        | --- | --- | --- |
        | ***Global errors*** |
        | 405 | METHOD NOT ALLOWED | ***METHOD_NOT_ALLOWED*** |
        | 404 | NOT FOUND | ***RESOURCE_NOT_FOUND*** |
        | 500 | INTERNAL SERVER ERROR | ***INTERNAL_SERVER_ERROR*** |
        | 403 | FORBIDDEN | ***ADMIN_RIGHTS_REQUIRED*** |
        | ***JSON validation errors*** |
        | 400 | BAD REQUEST | ***"field_name": "MISSING_FIELD"*** |
        | 400 | BAD REQUEST | ***"field_name": "UNKNOWN_FIELD"*** |
        | 400 | BAD REQUEST | ***"field_name": "INVALID_LENGTH"*** |
        | 400 | BAD REQUEST | ***"field_name": "INVALID_RANGE"*** |
        | 400 | BAD REQUEST | ***"field_name": "REGEXP_MISMATCH"*** |
        | 400 | BAD REQUEST | ***"field_name": "INVALID_TYPE"*** |
        | 400 | BAD REQUEST | ***"field_name": "NOT_NULLABLE"*** |
        | ***Formdata errors*** |
        | 400 | BAD REQUEST | ***FORMDATA_IS_EMPTY*** |
        | ***Query params errors*** |
        | 400 | BAD REQUEST | ***INVALID_FILTER_BY*** |
        | 400 | BAD REQUEST | ***INVALID_SELECT*** |
        | 400 | BAD REQUEST | ***INVALID_EXPAND*** |
        | 400 | BAD REQUEST | ***INVALID_PAGE_QUERY*** |
        | 400 | BAD REQUEST | ***INVALID_ID*** |
        | 400 | BAD REQUEST | ***NO_IDENTITY_PROVIDED*** |
        | ***File uploading errors*** |
        | 400 | BAD REQUEST | ***INVALID_JPG*** |
        | 400 | BAD REQUEST | ***INVALID_VIDEO*** |
        | 500 | INTERNAL SERVER ERROR | ***CANNOT_SAVE_FILE_ON_SERVER*** |
        | ***Authenticaton API errors*** |
        | 401 | UNAUTHORIZED | ***NO_TOKEN_PROVIDED*** |
        | 401 | UNAUTHORIZED | ***NO_FRESH_TOKEN_PROVIDED*** |
        | 401 | UNAUTHORIZED | ***TOKEN_HAS_EXPIRED*** |
        | 422 | UNPROCESSABLE ENTITY | ***INVALID_TOKEN*** |
        | 409 | CONFLICT | ***USERNAME_ALREADY_EXISTS*** |
        | 409 | CONFLICT | ***EMAIL_ALREADY_EXISTS*** |
        | 409 | CONFLICT | ***USERNAME_NOT_FOUND*** |
        | 409 | CONFLICT | ***INCORRECT_PASSWORD*** |
        | 403 | FORBIDDEN | ***USER_IS_BANNED*** |
        | 404 | NOT FOUND | ***USER_NOT_FOUND*** |
        | ***User API errors*** |
        | 409 | CONFLICT | ***USERNAME_ALREADY_EXISTS*** |
        | 409 | CONFLICT | ***EMAIL_ALREADY_EXISTS*** |
        | 404 | NOT FOUND | ***USERS_NOT_FOUND*** |
        | 404 | NOT FOUND | ***USER_NOT_FOUND*** |
        | ***Media API errors*** |
        | 404 | NOT FOUND | ***MEDIA_NOT_FOUND*** |
        | 404 | NOT FOUND | ***MEDIAS_NOT_FOUND*** |
        | 404 | NOT FOUND | ***VIDEO_NOT_FOUND*** |
        | ***Friends API errors*** |
        | 403 | FORBIDDEN | ***CANNOT_ADD_YOURSELF*** |
        | 403 | FORBIDDEN | ***ALREADY_REQUESTED*** |
        | 404 | NOT FOUND | ***FRIEND_NOT_FOUND*** |
        | 404 | NOT FOUND | ***FRIENDS_NOT_FOUND*** |
        | 404 | NOT FOUND | ***REQUEST_NOT_FOUND*** |
        | ***Video API errors*** |
        | 404 | NOT FOUND | ***UPLOAD_SESSION_NOT_FOUND*** |
        | 404 | NOT FOUND | ***TRANSCODE_SESSION_NOT_FOUND*** |
        | 404 | NOT FOUND | ***QUALITY_NOT_FOUND*** |
        | 404 | NOT FOUND | ***VIDEO_NOT_FOUND*** |
        | 400 | BAD REQUEST | ***NO_UPLOAD_SESSION_PROVIDED*** |
        | 400 | BAD REQUEST | ***NO_QUALITY_PROVIDED*** |
        | 409 | CONFLICT | ***SIZE_MISMATCH*** |
        | ***Genres API errors*** |
        | 404 | NOT FOUND | ***GENRE_NOT_FOUND*** |
        | 404 | NOT FOUND | ***GENRES_NOT_FOUND*** |
        | 404 | NOT FOUND | ***MEDIA_GENRE_NOT_FOUND*** |
        | 404 | NOT FOUND | ***MEDIA_GENRES_NOT_FOUND*** |
        | 409 | CONFLICT | ***SLUG_ALREADY_EXISTS*** |
        | 409 | CONFLICT | ***MEDIA_GENRE_ALREADY_EXISTS*** |
        | 404 | NOT FOUND | ***VIDEO_NOT_FOUND*** |
        | ***Rooms API errors*** |
        | 404 | NOT FOUND | ***ROOM_NOT_FOUND*** |
        | 404 | NOT FOUND | ***ROOMS_NOT_FOUND*** |
        | 409 | CONFLICT | ***ROOM_NAME_ALREADY_EXISTS*** |
        | 403 | FORBIDDEN | ***CREATOR_RIGHTS_REQUIRED*** |
        | ***Media Video API errors*** |
        | 404 | NOT FOUND | ***MEDIA_VIDEO_NOT_FOUND*** |
        | 404 | NOT FOUND | ***MEDIA_VIDEOS_NOT_FOUND*** |
        | 404 | NOT FOUND | ***LANGUAGE_NOT_FOUND*** |
        | 404 | NOT FOUND | ***VIDEO_NOT_FOUND*** |
        | 409 | CONFLICT | ***MEDIA_VIDEO_SOURCE_ALREADY_EXISTS*** |
        | ***Participant API errors*** |
        | 404 | NOT FOUND | ***PARTICIPANT_NOT_FOUND*** |
        | 404 | NOT FOUND | ***PARTICIPANTS_NOT_FOUND*** |
        | ***Sockets API errors*** |
        | null | error packet | ***ACCESS_TOKEN_REQUIRED*** |
        | ***Room Sockets API errors*** |
        | null | event: 'error' | ***ROOM_NOT_FOUND*** |
        | null | event: 'error' | ***USER_ALREADY_IN_SOME_ROOM*** |
        | null | event: 'error' | ***USER_NOT_IN_ROOM*** |
        | ***Playlist Media API errors*** |
        | 404 | NOT FOUND | ***PLAYLIST_MEDIAS_NOT_FOUND*** |
        | ***Playlist Media Sockets API errors*** |
        | null | event: 'error' | ***PLAYLIST_MEDIA_NOT_FOUND*** |
        | null | event: 'error' | ***ROOM_NOT_FOUND*** |
        | null | event: 'error' | ***MEDIA_NOT_FOUND*** |
        | null | event: 'error' | ***PLAYLIST_ORDER_OUT_OF_RANGE*** |
        | null | event: 'error' | ***SAME_PLAYLIST_MEDIA_ORDER*** |
        | null | event: 'error' | ***PLAYLIST_MEDIA_ALREADY_IN_ROOM*** |
        | null | event: 'error' | ***PLAYLIST_MEDIA_ALREADY_IN_PLAYER*** |
        | ***Video Player Sockets API errors*** |
        | null | event: 'error' | ***ROOM_NOT_FOUND*** |
        | null | event: 'error' | ***USER_NOT_IN_ROOM*** |
      </details>

      -----------------------------------------------

      <details>
        <summary> ***QUERY PARAMETER: filter_by*** </summary>

        ### Reserved values:
          - boolean value: `true`, `false`

          - none value: `null`


        ### Operators (must be in {} brackets):
          - equals: `{==}`

          - not equals: `{!=}`

          - more: `{>}`

          - less: `{<}`

          - more or equals: `{>=}`

          - less or equals: `{<=}`

          - value in list: `{in}`

          - value not in list: `{!in}`

          - like operator (words or substrings in value, NOT case-sensitive): `{~}`


        ### Only for boolean operators:
          - equals: `{==}`

          - not equals: `{!=}`


        ### Only for integer/float operators:
          - equals: `{==}`

          - not equals: `{!=}`

          - more: `{>}`

          - less: `{<}`

          - more or equals: `{>=}`

          - less or equals: `{<=}`


        ### Only for list operators:
          - value in list: `{in}`

          - value not in list: `{!in}`


        ### Only for string operators:
          - equals: `{==}`

          - not equals: `{!=}`

          - more: `{>}`

          - less: `{<}`

          - more or equals: `{>=}`

          - less or equals: `{<=}`

          - like operator (words or substrings in value, NOT case-sensitive): `{~}`


        ### list syntax (for value):
        `[val1;val2;val3]`


        ### Example:
          - `filter_by=name{~}Wolf wall street,id{in}[1;2;3],isBanned{==}false`

          - `filter_by=id{>=}15,avatar{!=}null`
      </details>
      
      -----------------------------------------------

      <details>
        <summary> ***SOCKETS: client-side events*** </summary>

        Description: `event` -> `data sent with event`


        ### Connection events:

          - `connected` -> `{'userId': int}`


        ### Room events:

          - `joined` -> `Participant entity`

          - `left` -> `Participant entity`

          - `roomUpdated` -> `Room entity`

          - `roomDeleted` -> `Room entity`


        ### Chat events:

          - `messageSent` -> `{'message': string, participant: Participant entity}`


        ### Playlist events:

          - `playlistMediaAdded` -> `PlaylistMedia entity`

          - `playlistMediaUpdated` -> `PlaylistMedia entity`

          - `playlistMediaSettedToPlayer` -> `PlaylistMedia entity`

          - `playlistMediaDeleted` -> `PlaylistMedia entity`


        ### Video Player events:

          - `syncPlayerState` -> `{'currentTime': int, 'isPaused': boolean}`

          - `sendPlayerStateFromClient` -> `{'userSID': string}` (on that event client must send request to `sendPlayerStateToUser` server event)
      </details>

      -----------------------------------------------

  
  version: 'MVP 1.0.0'

  license:
    name: Nginx 1.18.0
    url: https://nginx.org/LICENSE

externalDocs:
  description: ilow Gitlab!
  url: https://gitlab.eugenv.ru

tags:
  - name: User Authentication API
    description: Do everything with authentication :)
  - name: User API
    description: Do everything with users :)
  - name: Friends API
    description: Do everything with friends :)
  - name: Rooms API
    description: Do everything with rooms :)
  - name: Participants API
    description: Do everything with participants :)
  - name: Genres API
    description: Do everything with genres :)
  - name: Media API
    description: Do everything with media :)
  - name: Video API
    description: Do everything with videos :)
  - name: Media Video API
    description: Do everything with media videos :)
  - name: Playlist Media API
    description: Do everything with room playlist :)
  - name: Sockets API
    description: Do everything with sockets :)
  - name: Room Sockets API
    description: Do everything with room sockets :)
  - name: Playlist Media Sockets API
    description: Do everything with room playlist :)
  - name: Video Player Sockets API
    description: Do everything with video-player sockets :)
  - name: Chat Sockets API
    description: Do everything with chat sockets :)

################################################################################
#                            Hosts, Servers, cookies auth                      #
################################################################################

servers: [
  {
    "url": "https://ilow-api.eugenv.ru/",
    "description": "Production server"
  },
  {
    "url": "http://127.0.0.1:8302/",
    "description": "Local server"
  }
]

################################################################################
#                                    Paths                                     #
################################################################################

paths:
  /auth/signup:
    post:
      summary: Регистрация пользователя

      tags:
        - User Authentication API

      operationId: signup

      requestBody:
        required: true
        description: Обязательный JSON
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SipnupSchema'

      responses:
        200:
          description: Пользователь зарегистрироан, возвращается его сущность с access-token и refresh-token для cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /auth/login:
    post:
      summary: Логин пользователя

      tags:
        - User Authentication API

      operationId: login

      requestBody:
        required: true
        description: Обязательный JSON
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginSchema'

      responses:
        200:
          description: Пользователь залогинился, возвращается его сущность с access-token и refresh-token для cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /auth/logout:
    post:
      security:
        - cookieAuth: []

      operationId: logout

      summary: Выход из приложения, сброс токенов из cookies.

      tags:
        - User Authentication API

      responses:
        200:
          description: Выводит {logout = true}, сбрасывает токены из cookies

  /auth/refresh:
    post:
      security:
        - cookieAuth: []

      operationId: refresh

      summary: Запросить новый токен, когда истечёт срок старого

      description: |
        Когда `access-token` для авторизации истекает, используется `refresh-token` для его обновления.
        Если `refresh-token` истекает: необходимо заново залогиниться.

      tags:
        - User Authentication API

      responses:
        200:
          description: Выводит {refresh = true}, ставит новый токен в cookies

  /user/all:
    get:

      operationId: get_users

      summary: Получить пользователей

      tags:
        - User API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля пользователя, например `select=id,username`
        type: string
      - in: query
        name: expand
        required: false
        description: |
          Получить расширенные поля пользователя, `friends`, `createdRooms`.
        type: string
      - in: query
        name: filter_by
        required: false
        type: string
      - in: query
        name: limit
        required: false
        type: integer
      - in: query
        name: offset
        required: false
        type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /user:
    patch:
      security:
        - cookieAuth: []

      operationId: update_user

      summary: Пользователь обновляет данные аккаунта.

      tags:
        - User API

      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserUpdateSchema'

      responses:
        200:
          description: Пользователь обновляется, возвращается его сущность.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

    get:
      operationId: get_user

      summary: Получить пользователя

      tags:
        - User API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля пользователя, например `select=id,username`
        type: string
      - in: query
        name: expand
        required: false
        description: |
          Получить расширенные поля пользователя, `friends`, `createdRooms`.
        type: string
      - in: query
        name: id
        required: false
        description: Получить пользователя по id, принимается только id=число, иначе выведет ошибку.
        type: int
      - in: query
        name: username
        required: false
        description: Получить пользователя по username
        type: string

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_user

      summary: Удаление пользователя.

      description: Этим может воспользоваться и юзер без админ прав и админ, для удаления другого пользователя.

      tags:
        - User API

      parameters:
      - in: query
        name: id
        required: false
        description: Удалить пользователя по id, принимается только id=число, иначе выведет ошибку.
        type: int

      responses:
        200:
          description: Пользователь удаляется, возвращается его сущность.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /friends:
    get:
      operationId: get_friends

      summary: Получить друзей пользователя

      tags:
        - Friends API

      parameters:
        - in: query
          name: id
          required: true
          description: |
            id пользователя
          type: integer
        - in: query
          name: limit
          required: false
          type: integer
        - in: query
          name: offset
          required: false
          type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

    post:
      security:
        - cookieAuth: []

      operationId: add_friend

      summary: Отправить запрос дружбы

      tags:
        - Friends API

      parameters:
        - in: query
          name: friend_id
          required: true
          description: |
            id пользователя-друга
          type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_friend

      summary: Удалить пользователя из друзей

      tags:
        - Friends API

      parameters:
        - in: query
          name: friend_id
          required: true
          description: |
            id пользователя-друга
          type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /friends/received:
    get:
      security:
        - cookieAuth: []

      operationId: get_received_friend_requests

      summary: Получить запросы в друзья

      tags:
        - Friends API

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /friends/sent:
    get:
      security:
        - cookieAuth: []

      operationId: get_sent_friend_requests

      summary: Получить отправленные запросы в друзья

      tags:
        - Friends API

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /friends/delete:
    delete:
      security:
        - cookieAuth: []

      operationId: delete_sent_friend_request

      summary: Отменить запрос в друзья

      tags:
        - Friends API

      parameters:
        - in: query
          name: friend_id
          required: true
          description: |
            id пользователя-друга
          type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /friends/reject:
    delete:
      security:
        - cookieAuth: []

      operationId: delete_received_friend_request

      summary: Отклонить входящий запрос в друзья

      tags:
        - Friends API

      parameters:
        - in: query
          name: friend_id
          required: true
          description: |
            id пользователя-друга
          type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSchema'

  /media:
    post:
      security:
        - cookieAuth: []

      summary: |
        Создание медиа ***ADMIN ROLE REQUIRED***

      tags:
        - Media API

      operationId: create_media

      requestBody:
        required: true
        description: Обязательный JSON
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/MediaCreateSchema'

      responses:
        200:
          description: Объект медиа создан, возвращается его сущность.
          content:
            multipart/form-data:
              schema:
                $ref: '#/components/schemas/MediaSchema'

    get:
      summary: Получение медиа

      tags:
        - Media API

      operationId: get_media

      parameters:
        - in: query
          name: select
          required: false
          description: |
            Получить только определённые поля медиа, например `select=id,name`
          type: string
        - in: query
          name: id
          required: false
          description: Получить медиа по id, принимается только id=число, иначе выведет ошибку.
          type: int
        - in: query
          name: expand
          required: false
          description: |
            получить связанные сущности, доступно: `genres`, `videos`
          type: string

      responses:
        200:
          description: Возвращается сущность объекта медиа.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaSchema'

    patch:
      security:
        - cookieAuth: []

      operationId: update_media

      summary: |
        Обновление медиа ***ADMIN ROLE REQUIRED***

      tags:
        - Media API

      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/MediaUpdateSchema'

      parameters:
        - in: query
          name: id
          required: false
          description: Обновить media по id, принимается только id=число, иначе выведет ошибку.
          type: int

      responses:
        200:
          description: Объект медиа обновляется, возвращается его сущность.
          content:
            multipart/form-data:
              schema:
                $ref: '#/components/schemas/MediaSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_media

      summary: |
        Удаление медиа ***ADMIN ROLE REQUIRED***

      description: Этим может воспользоваться админ для удаления медиа

      tags:
        - Media API

      parameters:
      - in: query
        name: id
        required: false
        description: Удалить media по id, принимается только id=число, иначе выведет ошибку.
        type: int

      responses:
        200:
          description: Объект медиа удаляется, возвращается его сущность.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaSchema'

  /media/all:
    get:

      operationId: get_medias

      summary: Получить все медиа

      tags:
        - Media API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля медиа, например `select=id,name`
        type: string
      - in: query
        name: filter_by
        required: false
        type: string
      - in: query
        name: expand
        required: false
        description: |
          получить связанные сущности, доступно: `genres`, `videos`
        type: string
      - in: query
        name: genreIds
        required: false
        description: |
          Отфильтровать по жанрам (id через запятую)
        type: string
      - in: query
        name: limit
        required: false
        type: integer
      - in: query
        name: offset
        required: false
        type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaSchema'

  /upload/session:
    get:
      security:
        - cookieAuth: []

      operationId: get_session_uuid
      summary: |
        Получить uuid загрузочной сессии ***ADMIN ROLE REQUIRED***
      description: получение uuid сессии, если сессия некоторое время не использовалась (24 часа), она удаляется, берётся на каждую загрузку отдельно
      tags:
        - Video API

      responses:
          200:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SessionSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: abort_session
      summary: |
        отменить загрузочную сессию по её uuid ***ADMIN ROLE REQUIRED***
      tags:
        - Video API

      parameters:
      - in: query
        name: session
        required: true
        description: |
          uuid сессии

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AbortSchema'

  /upload:
    post:
      security:
        - cookieAuth: []
      
      operationId: load_chunk
      summary: |
        загрузить чанк файла ***ADMIN ROLE REQUIRED***
      tags:
        - Video API

      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ChunkSchema'

      responses:
        200:
          description: возвращается индекс загруженного чанка
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChunkUploadedSchema'

        201:
          description: при загрузке последнего чанка, возвращается имя загруженного файла (статус код тоже 200, сваггер не дал второй 200-й сделать)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileUploadedSchema'

  /upload/transcode_status:
    get:
      operationId: get_transcode_status
      summary: |
        получить статусы обработки видео ***ADMIN ROLE REQUIRED***
      tags:
        - Video API

      parameters:
      - in: query
        name: session
        required: true
        description: |
          uuid загрузочной сессии

      responses:
        200:
          description: |
            может также возвращать статус `expired` у некоторых качеств, если они были завершены после 24 часов
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusesSchema'
  
  /room:
    post:
      security:
        - cookieAuth: []

      summary: Создание комнаты

      tags:
        - Rooms API

      operationId: create_room

      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RoomCreateSchema'

      responses:
        200:
          description: Комната создана, возвращается её сущность.
          content:
            multipart/form-data:
              schema:
                $ref: '#/components/schemas/RoomSchema'

    get:
      summary: Получение комнаты

      tags:
        - Rooms API

      operationId: get_room

      parameters:
        - in: query
          name: id
          required: false
          description: Получить комнату по id
          type: int
        - in: query
          name: name
          required: false
          description: Получить комнату по name
          type: string
        - in: query
          name: select
          required: false
          description: |
            Получить только определённые поля комнаты, например `select=id,name`
          type: string
        - in: query
          name: expand
          required: false
          description: |
            Добавить связанные сущности, доступно `creator`, `participants`, `playlistMedia`
          type: string

      responses:
        200:
          description: Возвращается сущность комнаты.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoomSchemaExpanded'

    patch:
      security:
        - cookieAuth: []

      operationId: update_room

      summary: Обновление комнаты

      description: |
        только создатель может обновить комнату


        При обновлении также отправляется событие в комнату `roomUpdated` с сущностью комнаты

      tags:
        - Rooms API

      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RoomUpdateSchema'

      parameters:
        - in: query
          name: id
          required: false
          description: Обновить комнату по id
          type: int

      responses:
        200:
          description: Комната обновляется, возвращается её сущность.
          content:
            multipart/form-data:
              schema:
                $ref: '#/components/schemas/RoomSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_room

      summary: Удаление комнаты

      description: |
        Только создатель комнаты может удалить её


        При удалении также отправляется событие в комнату `roomDeleted` с сущностью комнаты

        комната в контексте сокетов тоже закрывается

      tags:
        - Rooms API

      parameters:
      - in: query
        name: id
        required: false
        description: Удалить комнату по id
        type: int

      responses:
        200:
          description: Комната удаляется, возвращается её сущность.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoomSchema'

  /room/all:
    get:
      operationId: get_rooms

      summary: Получить все комнаты

      tags:
        - Rooms API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля комнаты, например `select=id,name`
        type: string
      - in: query
        name: filter_by
        required: false
        type: string
      - in: query
        name: expand
        required: false
        description: |
          Добавить связанные сущности, доступно `creator`, `participants`, `playlistMedia`
        type: string
      - in: query
        name: limit
        required: false
        type: integer
      - in: query
        name: offset
        required: false
        type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoomSchemaExpanded'

  /genre/all:
    get:
      operationId: get_genres

      summary: Получить все жанры

      tags:
        - Genres API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля, например `select=id,name`
        type: string
      - in: query
        name: filter_by
        required: false
        type: string
      - in: query
        name: limit
        required: false
        type: integer
      - in: query
        name: offset
        required: false
        type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenreSchema'

  /media/genre:
    get:
      operationId: get_media_genres

      summary: Получить все жанры конректного медиа

      tags:
        - Genres API
        - Media API

      parameters:
      - in: query
        name: mediaId
        required: false
        description: |
          получить по id медиа
        type: string
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля, например `select=id,name`
        type: string

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenreSchema'

    post:
      security:
        - cookieAuth: []

      operationId: add_media_genre

      summary: |
        Добавить жанр к медиа ***ADMIN ROLE REQUIRED***

      tags:
        - Genres API
        - Media API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MediaGenreAddOrDeleteSchema'

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenreSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_media_genre

      summary: |
        Удалить жанр из медиа ***ADMIN ROLE REQUIRED***

      tags:
        - Genres API
        - Media API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MediaGenreAddOrDeleteSchema'

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenreSchema'

  /genre:
    post:
      security:
        - cookieAuth: []

      summary: |
        Создание жанра ***ADMIN ROLE REQUIRED***

      tags:
        - Genres API

      operationId: create_genre

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenreCreateSchema'

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenreSchema'

    get:
      summary: Получение жанра

      tags:
        - Genres API

      operationId: get_genre

      parameters:
        - in: query
          name: select
          required: false
          description: |
            Получить только определённые поля например `select=id,name`
          type: string
        - in: query
          name: id
          required: false
          description: Получить по id
          type: int

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenreSchema'

    patch:
      security:
        - cookieAuth: []

      operationId: update_genre

      summary: |
        Обновление жанра ***ADMIN ROLE REQUIRED***

      tags:
        - Genres API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenreUpdateSchema'

      parameters:
        - in: query
          name: id
          required: false
          description: Обновить по id
          type: int

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenreSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_genre

      summary: |
        Удаление жанра ***ADMIN ROLE REQUIRED***

      tags:
        - Genres API

      parameters:
      - in: query
        name: id
        required: false
        description: Удалить по id
        type: int

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenreSchema'
  
  /media/video:
    post:
      security:
        - cookieAuth: []

      summary: |
        Создание видео медиа ***ADMIN ROLE REQUIRED***

      tags:
        - Media Video API

      operationId: create_media_video

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MediaVideoCreateSchema'

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaVideoSchema'

    get:
      summary: Получение видео медиа

      tags:
        - Media Video API

      operationId: get_video_media

      parameters:
        - in: query
          name: select
          required: false
          description: |
            Получить только определённые поля медиа, например `select=id,name`
          type: string
        - in: query
          name: expand
          required: false
          description: |
            Добавить связанные сущности, пока доступно `expand=media`
          type: string
        - in: query
          name: id
          required: false
          description: Получить видео по id
          type: int

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaVideoSchema'

    patch:
      security:
        - cookieAuth: []

      operationId: update_video_media

      summary: |
        Обновление видео медиа ***ADMIN ROLE REQUIRED***

      tags:
        - Media Video API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MediaVideoUpdateSchema'

      parameters:
        - in: query
          name: id
          required: false
          description: Обновить видео по id
          type: int

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaVideoSchema'

    delete:
      security:
        - cookieAuth: []

      operationId: delete_video_media

      summary: |
        Удаление видео медиа ***ADMIN ROLE REQUIRED***

      tags:
        - Media Video API

      parameters:
      - in: query
        name: id
        required: false
        description: Удалить видео по id
        type: int

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaVideoSchema'

  /media/video/all:
    get:
      operationId: get_video_medias

      summary: Получить все видео медиа

      tags:
        - Media Video API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля, например `select=id,name`
        type: string
      - in: query
        name: filter_by
        required: false
        type: string
      - in: query
        name: expand
        required: false
        description: |
          Добавить связанные сущности, пока доступно `expand=media`
        type: string
      - in: query
        name: limit
        required: false
        type: integer
      - in: query
        name: offset
        required: false
        type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaVideoSchema'
  
  /participants:
    get:
      summary: Получение участника

      tags:
        - Participants API

      operationId: get_participant

      parameters:
        - in: query
          name: id
          required: false
          description: Получить участника по id
          type: int
        - in: query
          name: select
          required: false
          description: |
            Получить только определённые поля участника, например `select=id,name`
          type: string

      responses:
        200:
          description: Возвращается сущность участника.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParticipantSchema'

  /participants/all:
    get:
      operationId: get_participants

      summary: Получить всех участников

      tags:
        - Participants API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля участников, например `select=id,name`
        type: string
      - in: query
        name: filter_by
        required: false
        description: |
          ДОСТУПНО: `id`, `userId`, `roomId`
        type: string
      - in: query
        name: limit
        required: false
        type: integer
      - in: query
        name: offset
        required: false
        type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParticipantSchema'
  
  /connect:
    post:
      operationId: socket_connect

      summary: Подключение к сокетам

      tags:
        - Sockets API

      description: |
        обязателен параметр `withCredentials: true` при инициализации объекта `io`, чтобы передать access token.

        Иначе отправляется error packet с json `{'message': 'ACCESS_TOKEN_REQUIRED'}`
        
        если пользователь уже подключён к другой сокет сессии (в другом браузере или вкладке),
        то во эту сессию происходит emit события disconnect (и выход из комнаты, если он был к ней подключён).


      responses:
        200:
          description: |
            при успешном подключении происходит emit события `connected` с json 
          content:
            application/json:
              schema:
                type: object
                properties:
                  userId:
                    type: integer
                    example: 1

  /disconnect:
    post:
      operationId: socket_disconnect

      summary: Отключение от сокетов

      tags:
        - Sockets API

      description: |
        Происходит при событии `dicsonnect`, либо когда пользователь закрывает страницу.

        Пользователь автоматически удаляется из всех комнат.

        При удалении пользователя комнаты происходит emit события `left` в соотв. комнату
        с json сущностью участника.

  /join:
    post:
      operationId: join_room

      summary: Подключение к комнате

      description: |
        при успешном подключении происходит emit события `joined` в соотв. комнату с json сущностью участника

      tags:
        - Room Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoomIdSchema'

      responses:
        200:
          description: |
            при успешном подключении происходит emit события `joined` в соотв. комнату с json сущностью участника
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParticipantSchema'

  /leave:
    post:
      operationId: leave_room

      summary: Отключение от комнаты

      tags:
        - Room Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoomIdSchema'

      responses:
        200:
          description: |
            при успешном отключении происходит emit события `left` в соотв. комнату с json сущностью участника
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParticipantSchema'
  
  /playlist_media/all:
    get:
      operationId: get_all_playlist_media

      summary: Получить все плейлист-медиа

      tags:
        - Playlist Media API

      parameters:
      - in: query
        name: select
        required: false
        description: |
          Получить только определённые поля, например `select=id,name`
        type: string
      - in: query
        name: filter_by
        required: false
        description: |
          ДОСТУПНО: `id`, `mediaId`, `roomId`, `order`
        type: string
      - in: query
        name: limit
        required: false
        type: integer
      - in: query
        name: offset
        required: false
        type: integer

      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistMediaSchema'

  /addPlaylistMedia:
    post:
      operationId: add_playlist_media

      summary: Добавление медиа в комнату

      tags:
        - Playlist Media Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaylistMediaCreateSchema'

      responses:
        200:
          description: |
            при успешном создании происходит emit события `playlistMediaAdded` в соотв. комнату с json сущностью плейлист-медиа
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistMediaSchema'

  /updatePlaylistMedia:
    post:
      operationId: update_playlist_media

      summary: Обновить плейлист-медиа

      tags:
        - Playlist Media Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaylistMediaUpdateSchema'

      responses:
        200:
          description: |
            при успешном обновлении происходит emit события `playlistMediaUpdated` в соотв. комнату с json сущностью плейлист-медиа

            order можно cтавить только больше нуля, т.к. нулевой плелист-медиа является тем, который ставится в плеер, для этого отдельное событие `setPlaylistMediaToPlayer`
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistMediaSchema'

  /setPlaylistMediaToPlayer:
    post:
      operationId: set_to_player_playlist_media

      summary: Поставить плейлист-медиа в плеер

      tags:
        - Playlist Media Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaylistMediaIdSchema'

      responses:
        200:
          description: |
            при успешной обработке происходит emit события `playlistMediaSettedToPlayer` в соотв. комнату с json сущностью плейлист-медиа

            из списка плейлиста удаляется предыдущий плейлист-медиа с `order=0`

            текущий плейлист-медиа, который ставим в плеер, теперь будет `order=0`
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistMediaSchema'

  /deletePlaylistMedia:
    post:
      operationId: delete_playlist_media

      summary: Удалить плейлист-медиа

      tags:
        - Playlist Media Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaylistMediaIdSchema'

      responses:
        200:
          description: |
            при успешном удалении происходит emit события `playlistMediaDeleted` в соотв. комнату с json сущностью плейлист-медиа
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistMediaSchema'
  
  /requestPlayerState:
    post:
      operationId: request_player_state

      summary: Запросить состояние плеера

      tags:
        - Video Player Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestPlayerStateSchema'

      responses:
        200:
          description: |
            при успешном запросе происходит emit события `syncPlayerState` только юзеру с json состоянием плеера

            если участников в комнате нет, кроме нашего юзера, то отправляется по этому же событию дефолтное состояние плеера: `{'currentTime': 0, 'isPaused': true}`

            если есть другие участники, то случайному участнику отправляется событие `sendPlayerStateFromClient` с полем в json `userSID`
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlayerStateSchema'

  /sendPlayerStateToUser:
    post:
      operationId: send_player_state_to_user

      summary: Отправление состояния плеера участнику

      tags:
        - Video Player Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendPlayerStateToUserSchema'

      responses:
        200:
          description: |
            при успешном отправлении происходит emit события `syncPlayerState` юзеру по userSID с json состоянием плеера
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlayerStateSchema'

  /sendPlayerStateToEveryone:
    post:
      operationId: send_player_state_to_everyone

      summary: Отправление состояния плеера всем в комнату

      tags:
        - Video Player Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendPlayerStateToEveryoneSchema'

      responses:
        200:
          description: |
            при успешном отправлении происходит emit события `syncPlayerState` в соотв. комнату с json состоянием плеера
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlayerStateSchema'

  /sendMessage:
    post:
      operationId: send_message_to_room

      summary: Отправление сообщения в комнату

      tags:
        - Chat Sockets API

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessageSendSchema'

      responses:
        200:
          description: |
            при успешном отправлении происходит emit события `messageSent` в соотв. комнату с json сообщения
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageSentSchema'


################################################################################
#                                    Schemas                                   #
################################################################################


components:
  schemas:
    SipnupSchema:
      type: object
      properties:
        username:
          type: string
          nullable: false
          maxLength: 30
          minLength: 5
          description: |
            Уникальный username пользователя, имеет `regexp = ^[a-zA-Z0-9._-]+$`
          required: true
          example: "nick2004"
        password:
          type: string
          nullable: false
          minLength: 8
          description: |
            имеет `regexp = ^[a-zA-Z0-9.@_-]+$`
          required: true
          example: 'somepassword'
        email:
          type: string
          nullable: false
          minLength: 1
          required: true
          example: 'example@example.org'
        displayName:
          type: string
          nullable: false
          maxLength: 30
          minLength: 1
          description: Необязательно уникальное имя пользователя, которое будет показываться другим
          required: true
          example: 'Lolkek'

    LoginSchema:
      type: object
      properties:
        username:
          type: string
          nullable: false
          maxLength: 30
          minLength: 5
          description: |
            Уникальный username пользователя, имеет `regexp = ^[a-zA-Z0-9._-]+$`
          required: true
          example: "nick2004"

        password:
          type: string
          nullable: false
          minLength: 8
          description: |
            имеет `regexp = ^[a-zA-Z0-9.@_-]+$`
          required: true
          example: 'somepassword'

    UserUpdateSchema:
      type: object
      properties:
        username:
          type: string
          nullable: false
          maxLength: 30
          minLength: 5
          description: |
            Уникальный username пользователя, имеет `regexp = ^[a-zA-Z0-9._-]+$`
          required: false
          example: "nick2004"
        displayName:
          type: string
          minLength: 1
          maxLength: 30
          required: false
          example: 'Lolkek'
        email:
          type: string
          required: false
          minLength: 1
          example: 'example@example.org'
        password:
          type: string
          nullable: false
          minLength: 8
          description: |
            имеет `regexp = ^[a-zA-Z0-9.@_-]+$`
          required: true
          example: 'somepassword'
        birthday:
          type: integer
          required: false
          min: 86400
          description: 'timestamp'
          example: 1710230677
        description:
          type: string
          required: false
          minLength: 1
          maxLength: 140
          example: 'Описание страничкии'
        avatar:
          required: false
          type: string
          format: binary
          description: только .jpg

    UserSchema:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        username:
          type: string
          nullable: false
          example: 'nick2004'
        displayName:
          type: string
          nullable: false
          example: 'Lolkek'
        email:
          type: string
          nullable: false
          example: 'example@example.org'
        isBanned:
          type: boolean
          nullable: false
          example: false
        registrationDate:
          type: integer
          nullable: false
          description: 'timestamp'
          example: 1710230677
        birthday:
          type: integer
          nullable: true
          description: 'timestamp'
          example: 1710230677
        description:
          type: string
          nullable: true
          example: 'Описание страничкии'
        avatar:
          type: string
          nullable: true
          example: '/static/images/avatar_id_1.png'
        friends:
          type: string
          nullable: true

    MediaSchema:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        name:
          type: string
          nullable: false
          example: 'Titanic'
        description:
          type: string
          nullable: false
          example: 'Описание фильма'
        thumbnail:
          type: string
          nullable: false
          example: '/static/images/thumbnail_film.png'
        preview:
          type: string
          nullable: false
          example: '/static/images/preview_film.png'
        trailer:
          type: string
          nullable: true
          example: '/static/videos/abc1.mp4'

    MediaUpdateSchema:
      type: object
      properties:
        name:
          minLength: 1
          maxLength: 50
          type: string
          nullable: false
          example: 'Titanic'
          description: При пустом поле остаётся прежнее значение.
          required: false
        description:
          minLength: 1
          maxLength: 500
          type: string
          nullable: false
          example: 'Описание фильма'
          required: false
        thumbnail:
          required: false
          type: string
          format: binary
          description: только .jpg. При пустом поле остаётся прежнее значение.
        preview:
          required: false
          type: string
          format: binary
          description: только .jpg. При пустом поле остаётся прежнее значение.
        trailer:
          minLength: 3
          required: false
          type: string
          nullable: false
          example: 'abc1.mp4'
          description: |
            В поле вставляется имя файла с расширением. `regexp = ^[0-9a-z]+\.[a-z0-9]+$`

    MediaCreateSchema:
      type: object
      properties:
        name:
          minLength: 1
          maxLength: 50
          type: string
          nullable: false
          example: 'Titanic'
          required: true
        description:
          minLength: 1
          maxLength: 500
          type: string
          nullable: false
          example: 'Описание фильма'
          required: true
        thumbnail:
          nullable: false
          required: true
          type: string
          format: binary
          description: только .jpg
        preview:
          nullable: false
          required: true
          type: string
          format: binary
          description: только .jpg
        trailer:
          minLength: 3
          required: false
          type: string
          nullable: false
          example: 'abc1.mp4'
          description: |
            В поле вставляется имя файла с расширением. `regexp = ^[0-9a-z]+\.[a-z0-9]+$`

    ChunkSchema:
      type: object
      properties:
        session:
          type: string
          nullable: false
          required: true
          description: |
            Перед загрузкой необходимо получить uuid сессии `(/upload/session [GET])`
        totalFileSize:
          type: integer
          nullable: false
          required: true
          min: 1
        totalChunkCount:
          type: integer
          nullable: false
          required: true
          min: 1
        chunkIndex:
          type: integer
          nullable: false
          required: true
          min: 0
        chunkByteOffset:
          type: integer
          nullable: false
          required: true
          min: 0
        chunk:
          type: string
          format: binary
          required: true
          nullable: false
          description: |
            файл чанка, доступные форматы: `.mp4`, `.mov`, `.avi`, `.m4v`, `.mkv`

    SessionSchema:
      type: object
      properties:
        session:
          type: string
          nullable: false
          example: jrj2rij2oriej23i324
          description: uuid созданной сессии

    ChunkUploadedSchema:
      type: object
      properties:
        chunkUploaded:
          type: integer
          nullable: false
          example: 0
          description: индекс загруженного чанка

    FileUploadedSchema:
      type: object
      properties:
        video:
          type: string
          nullable: false
          example: video.mp4
          description: возвращается имя загруженного файла после последнего чанка

    AbortSchema:
      type: object
      properties:
        aborted:
          type: string
          nullable: false
          example: jrj2rij2oriej23i324
          description: uuid отменённой сессии

    StatusesSchema:
      type: object
      description: |
        может также возвращать статус `expired` у некоторых качеств, если они были завершены определённое время назад и удалены из сессии
      properties:
        360p:
          type: string
          nullable: false
          example: success
          description: статус успешного завершения обработки качества
        480p:
          type: string
          nullable: false
          example: error
          description: статус завершения обработки качества с ошибкой
        720p:
          type: string
          nullable: false
          example: processing
          description: статус идущей обработки качества
        1080p:
          type: string
          nullable: false
          example: pending
          description: статус ожидания обработки качества

    GenreSchema:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        name:
          type: string
          nullable: false
          example: 'Ужасы'
        slug:
          type: string
          nullable: false
          example: 'horror'

    GenreCreateSchema:
      type: object
      properties:
        name:
          type: string
          nullable: false
          required: true
          example: 'Ужасы'
          minLength: 2
          description: |
            имеет regexp `^[А-Я]{1}[а-я]+$`
        slug:
          type: string
          required: true
          nullable: false
          example: 'horror'
          minLength: 2
          description: |
            имеет regexp `^[a-z]+$`

    GenreUpdateSchema:
      type: object
      properties:
        name:
          type: string
          nullable: false
          required: false
          example: 'Ужасы'
          minLength: 2
          description: |
            имеет regexp `^[А-Я]{1}[а-я]+$`
        slug:
          type: string
          required: false
          nullable: false
          example: 'horror'
          minLength: 2
          description: |
            имеет regexp `^[a-z]+$`

    MediaGenreAddOrDeleteSchema:
      type: object
      properties:
        mediaId:
          type: integer
          nullable: false
          required: true
          example: 1
        genreId:
          type: integer
          nullable: false
          required: true
          example: 1
    
    RoomSchema:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        creatorId:
          type: integer
          nullable: false
          example: 1
        name:
          type: string
          nullable: false
          example: 'Room_1'
        title:
          type: string
          nullable: false
          example: 'комната жени'
        isPrivate:
          type: boolean
          nullable: false
          example: false
        cover:
          type: string
          nullable: true
          example: '/static/images/cover.jpg'

    RoomSchemaExpanded:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        creatorId:
          type: integer
          nullable: false
          example: 1
        name:
          type: string
          nullable: false
          example: 'Room_1'
        title:
          type: string
          nullable: false
          example: 'комната жени'
        isPrivate:
          type: boolean
          nullable: false
          example: false
        cover:
          type: string
          nullable: true
          example: '/static/images/cover.jpg'
        creator:
          $ref: '#/components/schemas/UserSchema'
          required: false

    RoomCreateSchema:
      type: object
      properties:
        name:
          type: string
          nullable: false
          example: 'Room_1'
          minLength: 1
          maxLength: 30
          description: |
            уникальное имя, `regexp = ^[A-Za-z0-9_]+$`
          required: true
        title:
          type: string
          nullable: false
          example: 'комната жени'
          minLength: 1
          maxLength: 20
          required: true
        isPrivate:
          type: boolean
          nullable: false
          example: false
          required: true
        cover:
          type: string
          format: binary
          required: false
          nullable: false
          description: формат .jpg

    RoomUpdateSchema:
      type: object
      properties:
        name:
          type: string
          nullable: false
          example: 'Room_1'
          minLength: 1
          maxLength: 30
          description: |
            уникальное имя, `regexp = ^[A-Za-z0-9_]+$`
          required: false
        title:
          type: string
          nullable: false
          example: 'комната жени'
          minLength: 1
          maxLength: 20
          required: false
        isPrivate:
          type: boolean
          nullable: false
          example: false
          required: false
        cover:
          type: string
          format: binary
          required: false
          nullable: false
          description: формат .jpg

    MediaVideoSchema:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        mediaId:
          type: integer
          nullable: false
          example: 1
        name:
          type: string
          nullable: false
          example: 'Titanic (Lostfim)'
        source:
          type: string
          nullable: false
          example: 'source.mp4'
        language:
          type: string
          nullable: false
          example: 'RU'

    MediaVideoCreateSchema:
      type: object
      properties:
        mediaId:
          type: integer
          nullable: false
          required: true
          example: 1
        name:
          minLength: 1
          type: string
          nullable: false
          required: true
          example: 'Titanic (Lostfim)'
        source:
          minLength: 3
          required: true
          type: string
          nullable: false
          example: 'source.mp4'
          description: |
            В поле вставляется имя файла с расширением. `regexp = ^[0-9a-z]+\.[a-z0-9]+$`
        language:
          type: string
          minLength: 2
          maxLength: 2
          required: true
          nullable: false
          example: 'RU'
          description: |
            двухзначный код страны для языка, валидируется на существование, также `regexp = ^[A-Z]{2}$`

    MediaVideoUpdateSchema:
      type: object
      properties:
        name:
          minLength: 1
          type: string
          nullable: false
          required: false
          example: 'Titanic (Lostfim)'
        source:
          minLength: 3
          required: false
          type: string
          nullable: false
          example: 'source.mp4'
          description: |
            В поле вставляется имя файла с расширением. `regexp = ^[0-9a-z]+\.[a-z0-9]+$`
        language:
          type: string
          minLength: 2
          maxLength: 2
          required: false
          nullable: false
          example: 'RU'
          description: |
            двухзначный код страны для языка, валидируется на существование, также `regexp = ^[A-Z]{2}$`
    
    ParticipantSchema:
      type: object
      properties:
        id:
          type: integer
          nullable: false
          example: 1
        roomId:
          type: integer
          nullable: false
          example: 1
        userId:
          type: integer
          nullable: false
          example: 1
        name:
          type: string
          nullable: false
          example: "User's displayName"
        avatar:
          type: string
          nullable: true
          example: '/static/images/j23jro23uo4i2rt4kjfr.jpg'
    
    RoomIdSchema:
      type: object
      properties:
        roomId:
          type: integer
          nullable: false
          example: 1
    
    PlaylistMediaSchema:
      type: object
      properties:
        roomId:
          type: integer
          nullable: false
          example: 1
        mediaId:
          type: integer
          nullable: false
          example: 1
        order:
          type: integer
          nullable: false
          example: 0
        name:
          type: string
          nullable: false
          example: 'media name'
        thumbnail:
          type: string
          nullable: false
          example: '/static/images/j23jro23uo4i2rt4kjfr.jpg'

    PlaylistMediaCreateSchema:
      type: object
      properties:
        roomId:
          type: integer
          nullable: false
          example: 1
          required: true
        mediaId:
          type: integer
          nullable: false
          example: 1
          required: true

    PlaylistMediaUpdateSchema:
      type: object
      properties:
        playlistMediaId:
          type: integer
          nullable: false
          example: 1
          required: true
          min: 1
        order:
          type: integer
          nullable: false
          example: 1
          required: true
          min: 1

    PlaylistMediaIdSchema:
      type: object
      properties:
        playlistMediaId:
          type: integer
          nullable: false
          example: 1
          required: true
          min: 1

    PlayerStateSchema:
      type: object
      properties:
        currentTime:
          type: integer
          nullable: false
          example: 0
          min: 0
        isPaused:
          type: boolean
          nullable: false
          example: true

    SendPlayerStateToUserSchema:
      type: object
      properties:
        userSID:
          type: string
          nullable: false
          example: 'wefFedkDLl'
          minLength: 1
          required: true
        currentTime:
          type: integer
          nullable: false
          example: 0
          min: 0
          required: true
        isPaused:
          type: boolean
          nullable: false
          example: true
          required: true

    SendPlayerStateToEveryoneSchema:
      type: object
      properties:
        roomId:
          type: integer
          nullable: false
          example: 1
          min: 1
          required: true
        currentTime:
          type: integer
          nullable: false
          example: 0
          min: 0
          required: true
        isPaused:
          type: boolean
          nullable: false
          example: true
          required: true

    RequestPlayerStateSchema:
      type: object
      properties:
        roomId:
          type: integer
          nullable: false
          example: 1
          min: 1
          required: true

    MessageSendSchema:
      type: object
      properties:
        roomId:
          type: integer
          nullable: false
          example: 1
          min: 1
          required: true   
        message:
          type: string
          nullable: false
          example: 'hello world!'
          minLength: 1
          required: true 

    MessageSentSchema:
      type: object
      properties:  
        message:
          type: string
          nullable: false
          example: 'hello world!'
          minLength: 1
        participant:
          $ref: '#/components/schemas/ParticipantSchema'
